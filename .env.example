# The humanize/deepDive server functions call Amazon Bedrock (Claude Haiku
# 4.5) via IAM credentials, not an API key -- run `aws configure` (or
# `aws sso login`) locally so the AWS SDK's default credential chain can
# find them. Optional: override the region (defaults to us-east-1).
# AWS_REGION=us-east-1
